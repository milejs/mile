'use client';

import { MileComponentProps } from "@milejs/types";

// import { useState } from "react";

export function Featured(props: MileComponentProps) {
  // const [count, setCount] = useState(0);
  return (
    <div className="" {...props}>
      <div className="">Featured {'2'}</div>
      {/* <button onClick={() => setCount(s => s + 1)}>Incr</button> */}
    </div>
  )
}
